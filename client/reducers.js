
// Define action types, action creators, and sub-reducers that your redux app will use to calculate the state. Use the connect function from react-redux to obtain slices of state and the dispatch method throughout your app


function reducer(state = {}, action) {
  switch (action.type) {
    default:
      return state;
  }
}

module.exports = reducer;
